<Window x:Class="MotorcycleShop.UI.Views.OrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:MotorcycleShop.UI.Controls"
        Title="Оформление заказа" Height="750" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#FAFAFA">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="{StaticResource SecondaryBrush}" Padding="20,15" CornerRadius="8">
            <TextBlock Text="Оформление заказа"
                       FontSize="24" FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Foreground="White"/>
        </Border>

        <ScrollViewer Grid.Row="1" Margin="0,20,0,0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Border Background="White" Padding="20" CornerRadius="8" Margin="0,0,0,20">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="2" BlurRadius="5" Opacity="0.2"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="Информация о покупателе"
                                   Style="{StaticResource SubHeaderStyle}"
                                   Margin="0,0,0,15"/>

                        <Grid Margin="0,10,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="ФИО:" FontWeight="SemiBold" Style="{StaticResource TextStyle}" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="0" Grid.Column="1"
                                     Text="{Binding CustomerName, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ModernTextBoxStyle}"
                                     Margin="5"
                                     Height="30"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Email:" FontWeight="SemiBold" Style="{StaticResource TextStyle}" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="1" Grid.Column="1"
                                     Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ModernTextBoxStyle}"
                                     Margin="5"
                                     Height="30"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Телефон:" FontWeight="SemiBold" Style="{StaticResource TextStyle}" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="2" Grid.Column="1"
                                     Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ModernTextBoxStyle}"
                                     Margin="5"
                                     Height="30"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Адрес:" FontWeight="SemiBold" Style="{StaticResource TextStyle}" VerticalAlignment="Top" Margin="0,8,0,0"/>
                            <TextBox Grid.Row="3" Grid.Column="1"
                                     Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ModernTextBoxStyle}"
                                     Margin="5"
                                     Height="60"
                                     TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto"/>

                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Комментарий:" FontWeight="SemiBold" Style="{StaticResource TextStyle}" VerticalAlignment="Top" Margin="0,8,0,0"/>
                            <TextBox Grid.Row="4" Grid.Column="1"
                                     Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ModernTextBoxStyle}"
                                     Margin="5"
                                     Height="60"
                                     TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Background="White" Padding="20" CornerRadius="8">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="2" BlurRadius="5" Opacity="0.2"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="Информация о заказе"
                                   Style="{StaticResource SubHeaderStyle}"
                                   Margin="0,0,0,15"/>

                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Общая сумма:" FontWeight="SemiBold" Style="{StaticResource TextStyle}" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="0" Grid.Column="1"
                                       Text="{Binding OrderTotal, StringFormat={}{0:C}}"
                                       FontWeight="Bold"
                                       FontSize="20"
                                       Foreground="Green"
                                       VerticalAlignment="Center"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="💳 Оплатить"
                    Command="{Binding PayCommand}"
                    Style="{StaticResource ModernButtonStyle}"
                    Width="140"
                    Height="40"
                    Margin="0,0,15,0"/>

            <Button Content="⬅ Назад"
                    Command="{Binding GoBackCommand}"
                    Style="{StaticResource ModernButtonStyle}"
                    Width="140"
                    Height="40"
                    Background="{StaticResource SecondaryBrush}"/>
        </StackPanel>
    </Grid>
</Window>