<Window x:Class="MotorcycleShop.UI.Views.PaymentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:MotorcycleShop.UI.Controls"
        Title="Оплата заказа" Height="700" Width="700"
        WindowStartupLocation="CenterScreen"
        Background="#FAFAFA">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="{StaticResource SecondaryBrush}" Padding="20,15" CornerRadius="8">
            <TextBlock Text="Оплата заказа"
                       FontSize="24" FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Foreground="White"/>
        </Border>

        <!-- Сумма к оплате -->
        <Border Grid.Row="1" Background="{StaticResource PrimaryLightBrush}" Padding="20,15" Margin="0,15,0,20" CornerRadius="8">
            <TextBlock Text="{Binding OrderTotal, StringFormat='Сумма к оплате: {0:C}'}"
                       FontSize="22"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Foreground="Green"/>
        </Border>

        <Border Grid.Row="2" Background="White" Padding="25" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="2" BlurRadius="5" Opacity="0.2"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Информация о карте"
                           Style="{StaticResource SubHeaderStyle}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20"/>

                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Номер карты:" FontWeight="SemiBold" Style="{StaticResource TextStyle}" VerticalAlignment="Center"/>
                    <TextBox x:Name="CardNumberTextBox" Grid.Row="0" Grid.Column="1"
                             Text="{Binding CardNumber, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource ModernTextBoxStyle}"
                             Margin="5"
                             Height="35"
                             MaxLength="19"
                             controls:PlaceholderHelper.Placeholder="XXXX XXXX XXXX XXXX"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Срок действия:" FontWeight="SemiBold" Style="{StaticResource TextStyle}" VerticalAlignment="Center"/>
                    <TextBox x:Name="ExpiryDateTextBox" Grid.Row="1" Grid.Column="1"
                             Text="{Binding ExpiryDate, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource ModernTextBoxStyle}"
                             Margin="5"
                             Height="35"
                             MaxLength="5"
                             controls:PlaceholderHelper.Placeholder="MM/YY"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="CVV:" FontWeight="SemiBold" Style="{StaticResource TextStyle}" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="2" Grid.Column="1"
                             Text="{Binding Cvv, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource ModernTextBoxStyle}"
                             Margin="5"
                             Height="35"
                             MaxLength="3"
                             controls:PlaceholderHelper.Placeholder="123"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Владелец карты:" FontWeight="SemiBold" Style="{StaticResource TextStyle}" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="3" Grid.Column="1"
                             Text="{Binding CardHolderName, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource ModernTextBoxStyle}"
                             Margin="5"
                             Height="35"
                             controls:PlaceholderHelper.Placeholder="ИМЯ ФАМИЛИЯ"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Кнопки -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,25,0,0">
            <Button Content="💳 Оплатить"
                    Command="{Binding PayCommand}"
                    Style="{StaticResource ModernButtonStyle}"
                    Width="140"
                    Height="40"
                    Margin="0,0,20,0"/>

            <Button Content="❌ Отмена"
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource ModernButtonStyle}"
                    Width="140"
                    Height="40"
                    Background="{StaticResource SecondaryBrush}"/>
        </StackPanel>
    </Grid>
</Window>