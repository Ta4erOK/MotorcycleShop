<Window x:Class="MotorcycleShop.UI.Views.OrderStatusWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:MotorcycleShop.UI.Controls"
        Title="Статус заказа" Height="800" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#FAFAFA">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="{StaticResource SecondaryBrush}" Padding="20,15" CornerRadius="8">
            <TextBlock Text="Статус заказа"
                       FontSize="24" FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Foreground="White"/>
        </Border>

        <!-- Поиск заказа -->
        <Border Grid.Row="1" Background="White" Padding="20" CornerRadius="8" Margin="0,15,0,20">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="2" BlurRadius="5" Opacity="0.2"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Номер заказа:" FontWeight="SemiBold" Style="{StaticResource TextStyle}" VerticalAlignment="Center"/>
                <TextBox x:Name="OrderNumberTextBox" Grid.Column="1"
                         Text="{Binding OrderNumber, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ModernTextBoxStyle}"
                         Margin="5"
                         Height="35"
                         controls:PlaceholderHelper.Placeholder="Введите номер заказа"/>

                <Button Grid.Column="2"
                        Content="🔍 Поиск"
                        Command="{Binding SearchCommand}"
                        Style="{StaticResource ModernButtonStyle}"
                        Width="100"
                        Height="35"
                        Margin="10,0,0,0"/>
            </Grid>
        </Border>

        <!-- Информация о заказе -->
        <Border Grid.Row="2" Background="White" Padding="20" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="2" BlurRadius="5" Opacity="0.2"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Статус:" FontWeight="SemiBold" Style="{StaticResource TextStyle}" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding CurrentOrder.Status, TargetNullValue='Не найден'}"
                           FontWeight="SemiBold" Foreground="Blue" VerticalAlignment="Center" Style="{StaticResource TextStyle}"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Дата заказа:" FontWeight="SemiBold" Style="{StaticResource TextStyle}" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding CurrentOrder.OrderDate, StringFormat='{}{0:dd.MM.yyyy HH:mm}', TargetNullValue='Не найдена'}"
                           VerticalAlignment="Center" Style="{StaticResource TextStyle}"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Сумма:" FontWeight="SemiBold" Style="{StaticResource TextStyle}" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding CurrentOrder.TotalAmount, StringFormat={}{0:C}, TargetNullValue='Не найдена'}"
                           FontWeight="Bold" Foreground="Green" VerticalAlignment="Center" Style="{StaticResource TextStyle}"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Состав заказа:" FontWeight="SemiBold" Style="{StaticResource TextStyle}" VerticalAlignment="Top" Margin="0,10,0,0"/>
                <ScrollViewer Grid.Row="3" Grid.Column="1" Margin="0,10,0,0" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding CurrentOrder.Items, TargetNullValue={}}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F5F5F5" Padding="8" Margin="0,2" CornerRadius="3">
                                    <TextBlock Text="{Binding Path=., StringFormat='{}{0.Motorcycle.Brand} {0.Motorcycle.Model} - {0.Quantity} шт. x {0.Price:C}'}"
                                               Style="{StaticResource TextStyle}"/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Сообщение -->
        <TextBlock Grid.Row="2"
                   Text="{Binding StatusMessage}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Top"
                   FontSize="16"
                   FontStyle="Italic"
                   Foreground="Gray"
                   Margin="0,10,0,0"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,25,0,0">
            <Button Content="🔄 Обновить"
                    Command="{Binding SearchCommand}"
                    Style="{StaticResource ModernButtonStyle}"
                    Width="140"
                    Height="40"
                    Margin="0,0,15,0"/>

            <Button Content="🛒 Новый заказ"
                    Command="{Binding NewOrderCommand}"
                    Style="{StaticResource AddToCartButtonStyle}"
                    Width="140"
                    Height="40"/>
        </StackPanel>
    </Grid>
</Window>