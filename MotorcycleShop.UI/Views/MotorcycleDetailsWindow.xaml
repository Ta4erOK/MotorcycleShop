<Window x:Class="MotorcycleShop.UI.Views.MotorcycleDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Детали мотоцикла" Height="700" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#FAFAFA">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="{StaticResource SecondaryBrush}" Padding="20,15" CornerRadius="8">
            <TextBlock Text="{Binding Motorcycle, StringFormat='{}{0.Brand} {0.Model}'}"
                       FontSize="24" FontWeight="Bold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Margin="0,0,0,0"
                       Foreground="White"/>
        </Border>

        <Grid Grid.Row="1" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Изображение мотоцикла -->
            <Border Grid.Column="0" Background="White" CornerRadius="8" Margin="0,0,10,0">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="2" BlurRadius="5" Opacity="0.2"/>
                </Border.Effect>
                <Image x:Name="MotorcycleImage"
                       Source="{Binding Motorcycle.ImagePath}"
                       Stretch="Uniform"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Margin="20"/>
            </Border>

            <!-- Информация о мотоцикле -->
            <ScrollViewer Grid.Column="1" Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="Характеристики"
                               Style="{StaticResource SubHeaderStyle}"/>

                    <Border Background="White" Padding="15" CornerRadius="5" Margin="0,5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Год выпуска:" FontWeight="SemiBold" Style="{StaticResource TextStyle}"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Motorcycle.Year}" Style="{StaticResource TextStyle}"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Объем двигателя:" FontWeight="SemiBold" Style="{StaticResource TextStyle}"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Motorcycle.EngineCapacity, StringFormat={}{0} cc}" Style="{StaticResource TextStyle}"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Пробег:" FontWeight="SemiBold" Style="{StaticResource TextStyle}"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Motorcycle.Mileage, StringFormat={}{0} км}" Style="{StaticResource TextStyle}"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Цвет:" FontWeight="SemiBold" Style="{StaticResource TextStyle}"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Motorcycle.Color}" Style="{StaticResource TextStyle}"/>

                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Цена:" FontWeight="SemiBold" Style="{StaticResource TextStyle}"/>
                            <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding Motorcycle.Price, StringFormat={}{0:C}}"
                                       Style="{StaticResource TextStyle}"
                                       FontSize="16"
                                       Foreground="Green"
                                       FontWeight="Bold"/>

                            <TextBlock Grid.Row="5" Grid.Column="0" Text="Описание:" FontWeight="SemiBold" Style="{StaticResource TextStyle}" Margin="0,10,0,0"/>
                            <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding Motorcycle.Description}"
                                       TextWrapping="Wrap"
                                       Style="{StaticResource TextStyle}"
                                       Margin="0,10,0,0"/>
                        </Grid>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="🛒 В корзину"
                    Command="{Binding AddToCartCommand}"
                    Style="{StaticResource AddToCartButtonStyle}"
                    Width="120"
                    Height="35"
                    Margin="0,0,15,0"/>

            <Button Content="⬅ Назад"
                    Command="{Binding GoBackCommand}"
                    Style="{StaticResource ModernButtonStyle}"
                    Width="120"
                    Height="35"
                    Background="{StaticResource SecondaryBrush}"/>
        </StackPanel>
    </Grid>
</Window>